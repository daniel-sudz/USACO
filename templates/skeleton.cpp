using namespace std;
#include <fstream>
#include <string>
#include <vector>
#include <cmath>
#include <algorithm>

using ll = long long;

int main() {
    ofstream fout("problem.out");
    ifstream fin("problem.in");

    int N;
    int M;
    fin >> N >> M;
}
